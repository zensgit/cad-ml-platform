# Core test lane dependencies — web framework + test runner ONLY.
#
# Deliberately EXCLUDES the heavy ML / serving / geometry stack (torch,
# transformers, qdrant-client, redis, open3d, trimesh, pythonocc, ...) so the
# import-light unit tests selected by `make test-core` collect and run in
# seconds on a stock virtualenv. This is the fast feedback lane for refactors
# and reviews; full local/CI runs still use requirements.txt + requirements-dev.txt.
#
# Pins mirror requirements.txt / requirements-dev.txt to avoid version drift.
# Test runner
pytest==7.4.4
pytest-asyncio==0.26.0
pytest-cov==4.1.0
httpx==0.28.1

# Web framework (lets src.api.* modules import without the serving stack)
fastapi==0.138.1
starlette==0.52.1
pydantic==2.13.4
pydantic-settings==2.14.2
uvicorn==0.49.0

# Light analysis libs pulled by core logic modules (NOT the ML stack)
numpy==1.26.4
prometheus-client==0.25.0
ezdxf==1.4.4
