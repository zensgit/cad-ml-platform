# DEV_PROVIDER_FRAMEWORK_DOCS_20260207

## Summary

Added a short operational + developer document describing the core provider framework (`src/core/providers/`) and its HTTP visibility endpoints.

## Changes

- `docs/PROVIDER_FRAMEWORK.md`
  - Documents: `BaseProvider`, `ProviderRegistry`, core bootstrap, built-in providers, and the provider registry/health endpoints.

## Validation

Docs-only change (no runtime behavior change).

