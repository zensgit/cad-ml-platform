# DEV_DEDUP2D_RUNTIME_SNAPSHOT_20251224

## Scope

- Document MinIO host port overrides for local staging.
- Capture the current local compose runtime snapshot for the dedup2d staging stack.

## Changes

- Updated `docs/DEDUP2D_STAGING_RUNBOOK.md` to note optional MinIO host port overrides.
- Added `docs/DEDUP2D_COMPOSE_RUNTIME_SNAPSHOT_20251224.md` with container and network
  snapshot details.

## Validation

- Not run (documentation-only changes).

## Notes

- Snapshot collected from the active docker stack on `cad-ml-network`.
