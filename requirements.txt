# Core Dependencies
fastapi==0.128.0
uvicorn[standard]==0.40.0
python-multipart==0.0.22
pydantic==2.12.5
pydantic-settings==2.12.0
Pillow==10.4.0

# CAD Processing
ezdxf==1.4.3
shapely==2.1.2
trimesh==4.11.1
numpy==1.26.4
scipy==1.17.0

# Machine Learning
scikit-learn==1.8.0
joblib==1.5.3
pandas==2.3.3
# tensorflow==2.14.0  # Optional, for deep learning models
# torch==2.1.0  # Optional, alternative to TensorFlow

# API & Web
httpx==0.28.1
httpcore==1.0.9
aiohttp==3.13.3
requests==2.32.5
PyJWT[crypto]==2.11.0
starlette==0.52.1
h11==0.16.0

# Database & Cache
redis==5.3.1
aioredis==2.0.1
# sqlalchemy==2.0.23  # Optional, for database support
# asyncpg==0.29.0  # Optional, for PostgreSQL

# Cloud Storage (Phase 4 Dedup2D File Storage)
boto3>=1.34.0  # AWS S3 / MinIO compatible storage
botocore>=1.34.0  # Required by boto3, also used for Stubber in tests

# Vector Store (Phase 1 Architecture Improvement)
qdrant-client>=1.7.0  # Production vector database, replaces custom FAISS+Redis

# Resilience & Retry (Phase 1 Architecture Improvement)
tenacity>=8.2.0  # Declarative retry logic, replaces manual retry loops

# Task Queue (Phase 2 Architecture Improvement)
arq>=0.25.0  # Redis-backed async task queue, replaces InMemoryMessageStore

# Observability (Phase 2 Architecture Improvement)
opentelemetry-api>=1.20.0  # OpenTelemetry tracing API
opentelemetry-sdk>=1.20.0  # OpenTelemetry SDK
# opentelemetry-exporter-otlp>=1.20.0  # Optional OTLP exporter
# opentelemetry-instrumentation-fastapi>=0.41b0  # Optional auto-instrumentation

# Geometry Features (Phase 2 Architecture Improvement)
# open3d>=0.17.0  # Optional, for advanced FPFH features (large dependency)

# Monitoring & Logging
prometheus-client==0.24.1
python-json-logger==2.0.7
loguru==0.7.3

# Testing
pytest==7.4.4
pytest-asyncio==0.26.0
pytest-cov==4.1.0
pytest-mock==3.15.1
httpx[http2]==0.28.1  # Keep http2 extra aligned

# Development Tools
black==24.10.0
flake8==6.1.0
mypy==1.19.1
pre-commit==3.8.0
ipython==8.38.0

# Utilities
python-dotenv==1.2.1
pyyaml==6.0.3
click==8.3.1
tqdm==4.67.2
matplotlib==3.10.8

# Data Validation
jsonschema==4.26.0  # For JSON schema validation

# Indirect dependency pinning
filelock==3.20.3
urllib3==1.26.20

# Optional: GPU Support
# onnxruntime-gpu==1.16.3  # For GPU inference
# cupy-cuda11x==12.2.0  # For CUDA operations
