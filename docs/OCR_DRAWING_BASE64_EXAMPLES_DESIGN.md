# OCR_DRAWING_BASE64_EXAMPLES_DESIGN

## Goal
- Provide quick copy-paste examples for OCR and drawing base64 endpoints in the README.

## Changes
- Added cURL examples for `/api/v1/ocr/extract-base64` and `/api/v1/drawing/recognize-base64`.

## Approach
- Use a minimal 1x1 PNG base64 payload and highlight optional filename/content_type hints.
