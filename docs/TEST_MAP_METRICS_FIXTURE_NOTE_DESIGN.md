# TEST_MAP_METRICS_FIXTURE_NOTE_DESIGN

## Goal
- Document the shared metrics fixtures for tests in the test map guide.

## Change
- Add a short Metrics 相关测试规范 section referencing fixtures in `tests/conftest.py`.

## Rationale
- Ensures new tests use consistent metrics gating to avoid environment-specific failures.
