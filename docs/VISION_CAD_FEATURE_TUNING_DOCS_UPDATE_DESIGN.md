# Vision CAD Feature Tuning Docs Update Design

## Overview
Document the new benchmark CLI options so tuning guidance matches the script
capabilities.

## Updates
- Add `--compare-json` and `--no-clients` to the tuning design doc.
- Clarify that comparison output is embedded in JSON exports.

## Validation
- Confirm the new flags are present in `docs/VISION_CAD_FEATURE_TUNING_DESIGN.md`.
