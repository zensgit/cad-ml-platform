# Vision CAD Feature Tuning Workflow Report Step Design

## Overview
Document the compare report step within the end-to-end tuning workflow so the
markdown summary is generated alongside compare and export outputs.

## Updates
- Added a compare report command using `vision_cad_feature_compare_report.py`.

## Files
- `docs/VISION_CAD_FEATURE_TUNING_DESIGN.md`
