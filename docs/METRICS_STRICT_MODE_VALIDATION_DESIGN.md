# METRICS_STRICT_MODE_VALIDATION_DESIGN

## Goal
- Validate strict metrics contract checks with STRICT_METRICS enabled.

## Approach
- Enable strict mode via environment variable.
- Run metrics contract tests to confirm strict thresholds and provider coverage.
