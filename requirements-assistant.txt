# Assistant API dependencies
# Install with: pip install -r requirements-assistant.txt

# Web frameworks (choose one)
fastapi>=0.100.0
uvicorn[standard]>=0.22.0

# Optional: Flask alternative
# flask>=2.3.0
# gunicorn>=21.0.0

# Data validation
pydantic>=2.0.0

# Async support
httpx>=0.24.0
aiofiles>=23.1.0

# Database (for SQLite backend)
# aiosqlite>=0.19.0

# Optional: sentence-transformers for better embeddings
# sentence-transformers>=2.2.0
# torch>=2.0.0

# DO NOT add `anthropic`, `openai`, or `dashscope` here before the
# assistant's provider SEAL lands
# (docs/development/L3_ASSISTANT_SCOPE_SEAL_DESIGNLOCK_20260805.md, #535).
# Their current absence is the only thing standing between
# ClaudeProvider/OpenAIProvider/QwenProvider,
# FunctionCallingEngine's default llm_provider="claude", and the
# _fallback_generate/get_best_available_provider auto-select paths, and a
# live, un-gated hosted-LLM call from a mounted, CD-deployed service
# (src/core/assistant/) that sits adjacent to customer drawing data. See
# #535 §0 findings #6, #13, #15;
# tests/unit/test_assistant_hosted_provider_dependency_lock.py enforces this
# for all three SDKs.
# anthropic>=0.30.0
# openai>=1.0.0
# dashscope>=1.14.0

# Monitoring and observability
prometheus-client>=0.17.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-mock>=3.11.0
