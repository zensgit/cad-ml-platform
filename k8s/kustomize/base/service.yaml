apiVersion: v1
kind: Service
metadata:
  name: cad-ml-api
  labels:
    app: cad-ml-api
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 8000
  selector:
    app: cad-ml-api
