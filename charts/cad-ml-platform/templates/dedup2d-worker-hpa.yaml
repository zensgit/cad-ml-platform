{{- if and .Values.dedup2d.worker.enabled .Values.dedup2d.worker.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "cad-ml-platform.fullname" . }}-dedup2d-worker
  labels:
    {{- include "cad-ml-platform.labels" . | nindent 4 }}
    app.kubernetes.io/component: dedup2d-worker
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "cad-ml-platform.fullname" . }}-dedup2d-worker
  minReplicas: {{ .Values.dedup2d.worker.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.dedup2d.worker.autoscaling.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.dedup2d.worker.autoscaling.targetCPUUtilizationPercentage }}
{{- end }}
