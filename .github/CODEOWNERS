# CODEOWNERS — L3 surfaces require an INDEPENDENT owner's review.
#
# ─────────────────────────────────────────────────────────────────────────────
#  ⚠️  THIS FILE ALONE IS NOT A GATE. Enabling code-owner review on it TODAY
#      would permanently DEADLOCK every L3 PR, because every rule below names
#      only @zensgit — the sole account — which cannot approve its own PRs.
#
#  Two prerequisites, in this order, BEFORE `require_code_owner_reviews = true`:
#
#   1. A genuinely INDEPENDENT reviewer with write access must exist AND be
#      added to every rule below, e.g.:
#          /scripts/eval_integrity_gate.py  @zensgit @independent-reviewer
#      This repo is a personal account (`zensgit/`), so a GitHub *organization
#      team* cannot be used without migrating the repo; add an independent
#      *collaborator* with write access instead.
#      NOTE: a second account controlled by the SAME person satisfies GitHub's
#      mechanical approval only — it does NOT satisfy the §4.7 "independent
#      critic" requirement. The reviewer must be a different person.
#
#   2. THEN set `require_code_owner_reviews = true` in branch protection.
#
#      Doing (2) before (1) is a foot-gun that locks the repo. Both are
#      owner-only actions; this PR touches neither.
# ─────────────────────────────────────────────────────────────────────────────
#
# Why (PRODUCT_STRATEGY.md §7.1): touching auth, authorization, customer data, an
# AI-output sink, a MODEL-RELEASE / MODEL-ACTIVATION surface, lifecycle write-back,
# or cross-system writes automatically promotes work to L3. Authors may not self-downgrade.
#
# This file exists because an unattended cadence loop authored a model-release gate
# (#509/#510/#511) and produced a FAKE-GREEN gate. §4.7 — "the author of a guard must not
# be the only author or reviewer of the discriminator that claims to prove it" — earned its
# place on the very first L3 attempt. An unattended loop cannot be both author and critic.

# --- Model ACTIVATION membrane (L3) ------------------------------------------
# The full boundary is broader than auto_retrain.sh. reload_model() has THREE call
# sites, all of which bypass the auto_retrain gate:
#   - src/api/v1/model.py:71                 (admin force hot-reload; ADMIN_TOKEN default "test")
#   - src/ml/monitoring/auto_remediation.py:301  (async auto rollback — no human in the loop)
#   - scripts/finetune_from_feedback.py:302  (force=True reload after retrain)
# plus the startup GRAPH2D_MODEL_PATH load in src/main.py.
/src/api/v1/model.py                  @zensgit
/src/ml/classifier.py                 @zensgit
/src/ml/monitoring/auto_remediation.py @zensgit
/src/main.py                          @zensgit

# --- Retraining / evaluation-integrity gate (L3) -----------------------------
# GLOB patterns, not exact filenames: an unattended loop already escaped an exact
# name (scripts/track_e_manifest.py slipped past track_e_eval_integrity.py). A new
# scripts/track_e_*.py or scripts/finetune_*.py must NOT be able to dodge ownership.
# (CODEOWNERS uses gitignore-style matching, so a trailing `*` is REQUIRED — a bare
#  `/scripts/track_e_` would only match a file literally named `track_e_`.)
/scripts/auto_retrain.sh              @zensgit
/scripts/eval_integrity_gate.py       @zensgit
/scripts/track_e_*                    @zensgit
/scripts/finetune_*                   @zensgit
/scripts/append_reviewed_to_manifest.py @zensgit

# --- Training-data governance (L3) -------------------------------------------
/scripts/ci/check_training_data_governance.py @zensgit
/scripts/ci/prune_safety_check.py     @zensgit
/data/manifests/                      @zensgit

# --- Authentication / authorization (L3) -------------------------------------
/src/api/dependencies.py              @zensgit
/src/api/middleware/                  @zensgit

# --- CI: the gates themselves (L3) -------------------------------------------
/.github/workflows/                   @zensgit
/.github/CODEOWNERS                   @zensgit

# --- Canonical product strategy ----------------------------------------------
/docs/PRODUCT_STRATEGY.md             @zensgit
