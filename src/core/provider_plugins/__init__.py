"""Provider plugin modules.

This package is intentionally separate from `src/core/providers/*` so teams can
add experimental providers with minimal merge risk and load them via
`CORE_PROVIDER_PLUGINS` without editing the built-in bootstrap list.
"""

